// src/setupTests.ts
import '@testing-library/jest-dom';

// Puedes añadir mocks globales aquí si es necesario
// Por ejemplo, mockear fetch o localStorage si tu app los usa globalmente
// jest.mock('axios'); // Si quieres mockear axios globalmente para todas las pruebas
